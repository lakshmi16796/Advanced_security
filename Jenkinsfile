pipeline {
  agent any
  stages 
{  
    stage ("User Input Stage")
    {
	    
      steps {	 
	      
	script {
		
                 echo "selected parameter is"	
		 if(env.choices == "Docker" && "QT")
		{
		 echo "Selected parameter is" 	
	         echo "${env.choices}"
		
		
			
		
		}
	    	} 
  }
  }
  }
  
  }
